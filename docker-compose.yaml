services:

  api-placemarkers-collection:
    image: api-placemarkers-collection-image
    container_name: api-placemarkers-collection
    build:
      context: .
      dockerfile: ./Docker/RR/Dockerfile
    volumes:
      - ./app:/var/www/html
    environment:
      APP_ENV: dev
      APP_DEBUG: 1
      APP_SECRET: 75914121030ad559bbae482519258ce1
      APP_RUNTIME: Baldinof\RoadRunnerBundle\Runtime\Runtime
      VAR_DUMPER_FORMAT: 'server'
      VAR_DUMPER_SERVER: 'buggregator:9912'
      MONGODB_URL: mongodb://root:root@cs-mongo:27017
      MONGODB_DB: placemarkers
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=place_markers_net"
      - "traefik.http.routers.api-placemarkers-collection.rule=Host(`api.placemarkers.local`) && PathPrefix(`/collections`)"
      - "traefik.http.routers.api-placemarkers-collection.entrypoints=web"
      - "traefik.http.routers.api-placemarkers-collection.priority=100"
      - "traefik.http.services.api-placemarkers-collection.loadbalancer.server.port=8080"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - place_markers_net

  api-placemarkers-collection-php-cli:
    image: api-placemarkers-collection-php-cli-image
    build:
      dockerfile: ./Docker/cli/Dockerfile
    container_name: api-placemarkers-collection-php-cli
    volumes:
      - ./app:/var/www/html
    environment:
      APP_ENV: dev
      APP_DEBUG: 1
      APP_SECRET: 75914121030ad559bbae482519258ce1
      MONGODB_URL: mongodb://root:root@cs-mongo:27017
      MONGODB_DB: placemarkers
    networks:
      - place_markers_net

networks:
  place_markers_net:
    external: true
